#include <SDL2/SDL.h>

int main()
{
    SDL_Window *win = SDL_CreateWindow(
        "stingray3d",
        SDL_WINDOWPOS_CENTERED,
        SDL_WINDOWPOS_CENTERED,
        100, 100, 0);

    while (true)
    {
    }

    return 0;
}